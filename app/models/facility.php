<?php
    
    class facility{
        /*
        var $id;
        var $name;
        var $cost;
        var $max_capacity;
        private $db_conn;
        
        public function __construct() {
            $this->db_conn = get_db_connection();
        }
        
        public function get_facility($param) {
            $this->query_db_facility($param);
        }
        
        private function query_db_facility($param) {
            $stmt = $this->db_conn->prepare("SELECT * FROM `facility` WHERE `FacilityNum` = ?");
            if ($stmt->execute(array($param))) {
                $row = $stmt->fetch();
                $this->id = $row["FacilityNum"];
                $this->name = $row["FacilityName"];
                $this->cost = $row["FacilityCost"];
                $this->max_capacity = $row["MaxCapacity"];
            }
        }
        */
    }
?>