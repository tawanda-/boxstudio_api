<?php
    
    class Activity{
        /*
        var $id;
        var $name;
        var $type;
        private $db_conn;
        
        public function __construct() {
            $this->db_conn = get_db_connection();
        }
        
        public function get_activity($param){
            $this->query_db_activity($param);
        }
        
        private function query_db_activity($param){
            $stmt = $this->db_conn->prepare("SELECT * FROM `activity` WHERE `actvityID` = ?");
            if ($stmt->execute(array($param))) {
                $row = $stmt->fetch();
                $this->id = $row["actvityID"];
                $this->name = $row["actvityName"];
                $this->type = $row["actvityType"];
            }
        }
        */
    }
?>